{
  "name": "trending-collector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "collect": "bun run src/cli.ts collect",
    "publish": "bun run src/cli.ts publish",
    "verify:growth": "bun run src/cli.ts verify-growth"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
